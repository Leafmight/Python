from mypackage.classes import course

class DataSheet():
    def __init__(self, courses):
        self.courses = []
        for course in courses:
            new_course = course(paragraph_lines)
            self.paragraphs.append(new_pragraph)